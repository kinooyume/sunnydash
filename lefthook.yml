pre-commit:
  parallel: true
  jobs:
    - name: eslint
      glob: "*.{js,ts,svelte}"
      run: bunx eslint --max-warnings 0 {staged_files}

commit-msg:
  jobs:
    - name: conventional-commit
      run: cog verify --file {1}
